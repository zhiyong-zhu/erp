# ERP 项目下一阶段 TODO

更新时间：2026-06-27

状态说明：
- `[ ]` 未开始
- `[-]` 进行中
- `[x]` 已完成

优先级说明：
- `P0`：上线前必须闭环
- `P1`：上线后首轮迭代必须补齐
- `P2`：增强体验、效率或长期稳定性

## P0：先把系统跑稳

### 1. 生产部署与环境闭环
- `[-]` ECS 部署流水线真实跑通
  验收：`main` 分支 push 后能完成后端构建、Web 构建、上传、部署、健康检查。
- `[ ]` 手动 `skip_build` 部署验证
  验收：GitHub Actions 手动触发 `skip_build=true` 时，不重新构建，不上传新包，只重装配置并重启 ECS 现有产物。
- `[ ]` ECS 首次初始化脚本实机验证
  验收：`scripts/setup-ecs.sh` 在干净 Ubuntu ECS 上可完成 JDK、PostgreSQL、Redis、RustFS、Nginx、systemd 配置。
- `[ ]` RustFS bucket 与上传链路验证
  验收：产品图片上传成功，返回 URL 可访问，bucket 不存在时初始化脚本能创建。
- `[ ]` 生产环境变量整理
  验收：数据库、Redis、JWT、RustFS、RabbitMQ 等敏感配置不写死在代码和提交文件中。
- `[ ]` 部署回滚演练
  验收：故意部署一个不可启动 JAR 后，`scripts/deploy.sh` 能回滚到上一版本并恢复服务。

### 2. 开发环境一键启动
- `[ ]` 恢复并提交 Docker Compose 开发环境
  验收：一条命令启动 PostgreSQL、Redis、RabbitMQ、RustFS。
- `[ ]` 补充开发环境 README
  验收：新机器按文档能完成依赖服务启动、后端启动、Web 启动。
- `[ ]` 本地空库初始化验证
  验收：PostgreSQL 空库启动后 Liquibase 全量迁移成功，后端可登录。

### 3. 当前 TODO 与文档债务清理
- `[x]` 重写 `TASK_TODO.md`，移除历史冲突标记
- `[ ]` 对齐 README、AGENTS、实际命令
  验收：Node/pnpm 版本、Liquibase 路径、启动命令与当前仓库实际一致。
- `[ ]` 梳理生产部署文档
  验收：包含 GitHub Secrets、ECS 初始化、首次部署、回滚、常见故障排查。

## P0：核心业务上线缺口

### 4. 认证、权限与审计
- `[-]` 业务模块数据权限覆盖
  范围：产品、原料、采购、库存、销售、生产。
  验收：普通业务角色不能越权查看其他部门或他人数据。
- `[-]` 敏感字段权限覆盖
  范围：产品成本价、库存成本、客户敏感信息、财务金额字段。
  验收：无权限用户接口和页面都不能看到敏感字段。
- `[ ]` 审计报表
  验收：能按用户、模块、动作、时间范围查询关键操作记录。
- `[ ]` 密码与会话策略
  验收：支持强密码规则、多端退出、禁用用户强制下线、过期 token 清理。

### 5. 库存真实账闭环
- `[-]` 高价值品批次追溯
  验收：能从原料批次追到生产批次，再追到成品序列号和销售出库。
- `[ ]` 并发库存压测
  验收：并发入库、出库、调拨、盘点时余额、流水、来源单据一致。
- `[ ]` 库存异常单闭环
  验收：缺货、错发、盘盈盘亏、批次异常能形成异常单、处理记录和审计留痕。

### 6. 采购闭环补齐
- `[-]` 采购审批规则
  范围：金额超限、价格超限、供应商黑名单。
  验收：触发规则后必须进入审批，审批通过前不能收货入库。
- `[ ]` 采购发票与付款联动
  验收：采购入库后可关联进项发票，形成应付、已付、未付统计。
- `[ ]` 供应商绩效评分
  验收：按交期、质量、价格波动、异常次数生成评分。

### 7. 销售闭环补齐
- `[-]` 销售异常处理
  范围：缺货、错发、退货差异、客户信用异常。
  验收：异常能进入处理流程，并联动库存、应收或退款。
- `[ ]` 销售发票与应收闭环
  验收：销售订单、发货、销项发票、应收、回款状态可追踪。
- `[ ]` 销售报表增强
  验收：支持趋势、客户排行、产品排行、渠道排行、毛利分析。

### 8. 生产闭环补齐
- `[-]` 排产能力
  验收：支持按工单、产线、日期安排生产计划。
- `[ ]` MRP 运算
  验收：根据销售需求、库存、安全库存、BOM 生成物料需求建议。
- `[ ]` 生产异常闭环
  验收：报废、返工、少料、超耗能形成异常记录并影响库存和成本。

## P1：首轮上线后必须补

### 9. 电商集成
- `[ ]` 平台授权框架
  范围：淘宝/天猫、京东、拼多多、抖音预留统一授权模型。
- `[ ]` 电商订单真实同步
  验收：从真实平台拉单，生成销售订单并保留平台单号。
- `[ ]` 库存同步
  验收：ERP 库存变化后能同步到平台，失败有重试和告警。
- `[ ]` 物流回传
  验收：ERP 发货后能回传平台物流单号。
- `[ ]` 电商对账
  验收：平台订单、ERP 销售单、收款、退款能对齐。

### 10. 财务与报税
- `[ ]` 进项发票台账
- `[ ]` 销项发票台账
- `[ ]` 应收应付账龄表
- `[ ]` 税额汇总表
- `[ ]` 报税申报表导出
- `[ ]` 财务字段权限和审计

### 11. Desktop 与 Mobile 现场能力
- `[ ]` Desktop 本地打印机直连
  验收：标签打印无需浏览器打印对话框，支持打印机配置和失败重试。
- `[ ]` Mobile 扫码入库、出库、盘点
  验收：移动端扫码能完成仓库现场核心操作。
- `[ ]` 离线缓存
  验收：弱网下可暂存现场操作，恢复网络后同步。
- `[ ]` 通知与待办
  验收：审批、异常、缺货、同步失败能推送到对应角色。

### 12. 报表、导出与打印
- `[ ]` 经营 Dashboard
  验收：老板视角看到销售、采购、库存、生产、财务核心指标。
- `[ ]` Excel 导出统一能力
  验收：核心列表支持按筛选条件导出。
- `[ ]` 后端 PDF 模板
  验收：采购单、销售单、入库单、出库单、发票可生成 PDF。
- `[ ]` 打印模板管理
  验收：业务单据和标签模板可维护版本。

## P1：质量与工程化

### 13. 自动化测试
- `[ ]` 后端核心服务测试补齐
  范围：采购、库存、销售、生产、权限。
- `[ ]` API 集成测试
  验收：从登录到核心业务流能自动跑通。
- `[ ]` Web 关键页面冒烟
  范围：登录、产品、采购、库存、销售、生产。
- `[ ]` Liquibase 空库迁移 CI
  验收：CI 中启动 PostgreSQL 并验证全量迁移。

### 14. 可观测性
- `[ ]` 结构化业务日志
- `[ ]` Actuator 指标暴露
- `[ ]` 部署失败告警
- `[ ]` 业务异常告警
- `[ ]` 慢接口和错误率监控

## P2：体验与长期能力

### 15. 编码规则与基础配置
- `[ ]` 单据编号规则配置
- `[ ]` 多仓库、多货位配置优化
- `[ ]` 数据字典维护体验优化
- `[ ]` 附件与图片统一管理

### 16. 性能与数据治理
- `[ ]` 大列表分页和索引复查
- `[ ]` 库存流水归档策略
- `[ ]` 操作日志归档策略
- `[ ]` 图片和附件生命周期策略

### 17. 产品体验
- `[ ]` 表格列配置和保存
- `[ ]` 常用筛选保存
- `[ ]` 批量操作体验统一
- `[ ]` 错误提示和空状态统一

## 建议执行顺序

1. 跑通 ECS 真实部署和回滚演练。
2. 补回 Docker Compose 开发环境，降低本地验证成本。
3. 做一次空库迁移和登录到核心页面的端到端验证。
4. 优先补权限、库存真实账、采购审批、销售发票这些上线风险点。
5. 再进入电商、财务、移动扫码、打印这些业务扩展。
